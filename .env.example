# Copy to .env and fill in (never commit .env)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional: enables the internal /test page (password is still embedded in the client bundle if set)
# VITE_TEST_PAGE_PASSWORD=
